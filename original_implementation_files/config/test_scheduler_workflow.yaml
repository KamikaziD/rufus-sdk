workflow_type: "TestScheduler"
description: "Test workflow for scheduler verification"
initial_state_model: "scheduled_report_models.ScheduledReportState"

steps:
  - name: "Generate_PDF"
    type: "STANDARD"
    function: "steps.scheduled.generate_report"
    automate_next: true
