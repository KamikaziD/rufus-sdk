# Minimal dev/test requirements (lightweight) for running unit tests locally
# Exclude heavy LLM / embedding libraries (pydantic-ai, sentence-transformers, etc.)
# Use this in CI/dev when you only need to run pytest and fastapi-related tests.

fastapi==0.109.0
pydantic==2.12.4
pytest==9.0.2
pytest-asyncio==0.21.0
pytest-env==1.2.0
redis==5.0.1
fakeredis==2.33.0
celery==5.5.3
asyncpg==0.31.0
psycopg2-binary==2.9.11
python-dotenv==1.0.0
uvicorn==0.27.0
pytest-celery==5.3.1