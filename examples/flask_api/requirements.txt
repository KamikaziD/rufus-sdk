# Flask API Example - Requirements

# Web framework
Flask==3.0.0
flask-cors==4.0.0

# Rufus SDK (install from parent directory)
# pip install -e ../..

# Database
psycopg2-binary==2.9.9

# Performance optimizations (Phase 1)
orjson>=3.9.0  # 3-5x faster JSON serialization
uvloop>=0.19.0  # 2-4x faster async I/O (automatically used by Rufus)

# YAML parsing (included in Rufus, but explicit here)
PyYAML==6.0.1

# Production server (optional)
gunicorn==21.2.0
