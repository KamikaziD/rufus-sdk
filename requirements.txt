# Rufus Edge SDK - Core Dependencies
# Minimal dependencies for fintech edge computing

# Core
pydantic>=2.0.0
PyYAML>=6.0
Jinja2>=3.1.2
typer>=0.21.0
click>=8.0.0
python-multipart

# Async support
aiosqlite>=0.19.0
anyio>=4.0.0

# JSON serialization (fast)
orjson>=3.9.0

# Event loop optimization
uvloop>=0.19.0

# HTTP client (for HTTP steps and edge check-in)
httpx>=0.25.0

# WebSocket client (for real-time device commands)
websockets>=12.0

# System monitoring (for device health checks)
psutil>=5.9.0
celery[redis]>=5.6.3  # For Celery-based workflow execution backend (optional)

# AI Inference (optional - for edge AI workflows)
# Install one or both depending on your models:
#   pip install onnxruntime           # For ONNX models (CPU)
#   pip install tflite-runtime        # For TFLite models (lightweight)
#   pip install tensorflow            # For TFLite models (full TensorFlow)
# Note: numpy is required by both inference runtimes
numpy>=1.24.0

# Server (optional - for cloud control plane)
fastapi>=0.100.0
uvicorn>=0.20.0
starlette>=0.27.0
slowapi>=0.1.9

# PostgreSQL (optional - for cloud control plane)
asyncpg>=0.27.0
psycopg2-binary>=2.9.0   # psycopg2 used by worker_registry.py (Celery fleet mgmt)

# Redis (required - used by events.py, observability/events.py, rufus_server/main.py)
redis>=5.0.0

# Database ORM and Migrations
sqlalchemy>=2.0.0
alembic>=1.13.0

# CLI formatting
rich>=13.0.0

# NATS JetStream + Protobuf (optional transport layer)
nats-py>=2.3             # PyPI package name; imported as `nats`
betterproto>=0.9.0
protobuf>=4.21

# Knowledge base / RAG (optional - builder_ai)
rank-bm25>=0.2.2         # BM25 re-ranking in KnowledgeBase.retrieve(); falls back gracefully if absent

# Fast typed serialization (optional — used by msgspec codec benchmark/path)
msgspec>=0.18.0

# WebAssembly runtime (optional — required for WASM step type and WASM benchmarks)
wasmtime>=12.0.0

# Python → WASM compiler (optional — required to rebuild apply_config.wasm; needs Python 3.11+)
py2wasm>=2.6.2

# Environment
python-dotenv>=1.0.0

# Scheduling (for cron expressions)
croniter>=2.0.0

# Encryption (for PCI-DSS compliance)
cryptography>=41.0.0

# JSON Schema validation (for command versioning)
jsonschema>=4.17.3,<5.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0   # 0.21.x breaks with pytest 9+ (AttributeError on FixtureDef.unittest)
pytest-cov>=4.0.0
pytest-mock>=3.12.0
asgi-lifespan>=2.1.0     # ASGI lifespan for integration tests (httpx + FastAPI)

# Type checking
typing_extensions>=4.0.0
annotated-types>=0.5.0
